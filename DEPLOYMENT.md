# Application Deployment Guide

## Docker

This application only supports deployment with Docker and Docker Compose

Build all service containers

```shell
sudo docker compose -f docker-compose.production.yml build
```

## Configuration

...
