#!/bin/bash

set -o errexit
set -o pipefail
set -o nounset

# collect static files
python /app/manage.py collectstatic --no-input

exec gunicorn config.asgi --worker-class asgi --bind 0.0.0.0:5000 --chdir=/app
